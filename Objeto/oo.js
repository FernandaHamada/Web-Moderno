// CÓDIGO NÃO EXECUTÁVEL
// Procedural

processamento(valor1, valor2, valor3) // funções que manipulam dados

// OO - dados personalidados(diz quais são os atributos)
objeto = {
    valor1,
    valor2,
    valor3,
    processamento(){
        //...
    }
}

objeto.processamento() // foco passou a ser o objeto

/* "mapaer o mundo em cima de objetos", mudança de foco*/

// Princípios importantes:
// 1 - abstração: traduzir um obj para o sistema, por necessidade de cada caso
// 2 - encapsulamento: detalhes de implementação ocultos, dentro do objeto. Interface de comunicação
// 3 - herança (prototype): recebe atributos ,comportamentos e caracteristicas de um objeto 'pai'
// 4 - polimorfismo: conceito generico, consegue substituir por conceitos concretos (multiplas formas) 